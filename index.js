
let btn = document.getElementById("btn");
        let speed = btn.value;
        btn.innerHTML = val;
        document.getElementById("result").innerHTML = "Value of the button: <b>" + speed+ "</b>";

if (speed<70) {
  prompt(Ok)
}
else  if (speed=>71++){
  console.log(Points=2);
}
else {
  console.log(Points=3);
}
